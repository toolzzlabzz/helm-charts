apiVersion: v1
kind: Service
metadata:
  name: {{ include "novu-chart.api.fullname" . }}
  labels:
    {{- include "novu-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
     {{- include "novu-chart.selectorLabels" . | nindent 4 }}
